export type MasterListYear = {
  effectiveYear: string;
  label: string;
  current: boolean;
};

export type OrgUnit = {
  orgUnitNo: string;
  orgUnitCode: string;
  orgUnitName: string;
};

export type Protocol = {
  code: string;
  name: string;
};
